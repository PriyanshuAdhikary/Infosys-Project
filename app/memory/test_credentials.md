"# Test Credentials



TaskSync uses \*\*mock authentication\*\* — the backend accepts any username/password.



\- Admin demo: `maya` / `demo123` → select \*\*Admin\*\* role toggle before submit

\- Member demo: `diego` / `demo123` → select \*\*Member\*\* role toggle before submit



Any other string combination will also succeed; role is chosen client-side via the toggle.

"

